[project]
name = "export-chapters"
version = "0.1.7"
description = "Export MP4/MOV chapter markers into YouTube description-friendly text"
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }
authors = [{ name = "Your Name" }]
dependencies = []

[project.scripts]
export-chapters = "export_chapters.cli:main"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
