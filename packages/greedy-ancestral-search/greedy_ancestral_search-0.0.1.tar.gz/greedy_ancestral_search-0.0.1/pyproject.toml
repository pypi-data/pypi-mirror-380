[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "greedy-ancestral-search"
version = "0.0.1"
dependencies = [
    "networkx>=3.4.2",
]
requires-python = ">=3.10"
authors = [
    { name = "Marc Franquesa Monés"},
    { name = "Jiaqi Zhang"},
    { name = "Caroline Uhler"},
]
description = "Python implementation of the Greedy Ancestral Seach (GAS) algorithm from our paper 'On the Number of Conditional Independence Tests in Constraint-based Causal Discovery'."
readme = "README.md"
license = "MIT"
license-files = ["LICENSE.md"]
keywords = ["causal discovery", "constraint-based causal discovery"]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

[project.urls]
Repository = "https://github.com/uhlerlab/greedy-ancestral-search"
Issues = "https://github.com/uhlerlab/greedy-ancestral-search/issues"

[tool.hatch.build.targets.sdist]
exclude = [
  "benchmarks",
]
[dependency-groups]
dev = [
    "causal-learn>=0.1.4.1",
    "conditional-independence>=0.1a6",
    "graphical-model-learning>=0.1a8",
    "graphical-models>=0.1a21",
    "ipykernel>=6.30.0",
    "matplotlib>=3.10.1",
    "numpy>=2.2.3",
    "pytest>=8.3.5",
    "ruff>=0.11.0",
]

[tool.pytest.ini_options]
testpaths = [
    "tests",
]

[tool.ruff]
lint.extend-select = ["I"]
