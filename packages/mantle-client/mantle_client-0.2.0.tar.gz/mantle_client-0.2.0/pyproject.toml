[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mantle-client"
version = "0.2.0"
description = "Private insights from research data - Client"
readme = "README.md"
requires-python = ">=3.12"
authors = [
    { name = "SBV", email = "secret@example.com" }
]
dependencies = [
    "requests>=2.32.0",
    "loguru>=0.7.2",
    "PyJWT>=2.10.0",
]

[tool.hatch.build]
only-packages = true