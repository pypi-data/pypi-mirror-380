[pytest]
testpaths = tests
addopts = -ra
asyncio_mode = auto
pythonpath = src

# Test markers for CI/local separation
markers =
    unit: Unit tests that can run in CI (no database dependencies)
    local_only: Integration tests requiring writable databases (local development only)
    integration: Integration tests (subset of local_only)
