# Core dependencies (minimal for the credibility tools)
# All dependencies are from Python standard library for credibility_tools.py itself

# Testing dependencies
pytest>=7.0.0
numpy>=1.20.0
pandas>=1.3.0

# Test harness dependencies (for IDE testing)
pyperclip>=1.8.0
pyautogui>=0.9.0
watchdog>=2.0.0
