[project]
name = "ntust-courses"
version = "0.2.0"
description = "NTUST Class Querier"
authors = [{ name = "xinshoutw" }]
dependencies = [
    "build>=1.3.0",
    "requests",
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.uv.workspace]
members = [
    "tmp",
]
