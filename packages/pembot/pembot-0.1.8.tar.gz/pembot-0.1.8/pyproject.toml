[build-system]
requires = ["flit_core >=3.11,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "pembot"
authors = [{name = "cyto", email = "aryan_sidhwani@protonmail.com"}]
license = "MIT"
license-files = ["LICENSE"]
dynamic = ["version", "description"]

[project.urls]
Home = "https://github.com/silverstone-git/pem-rag-chatbot"

#     "docs/",            # Exclude the entire 'docs' directory
#     "scratch_work/",    # Exclude the entire 'scratch_work' directory
#     "*.bak",            # Exclude files ending with .bak
#     "__pycache__/",     # Common to exclude, though often handled by .gitignore
#     "*.pyc",            # Also common, handled by .gitignore
# ]
