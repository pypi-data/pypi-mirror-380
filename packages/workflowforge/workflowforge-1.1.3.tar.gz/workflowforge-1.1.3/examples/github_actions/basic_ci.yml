# Do not modify - Generated with WorkflowForge
name: Basic CI Pipeline
on:
  push:
    branches:
      - main
jobs:
  test:
    steps:
      - uses: actions/checkout@v4
        name: Checkout code
      - run: echo 'Hello from WorkflowForge!'
        name: Say hello
    runs-on: ubuntu-latest
