[build-system]
requires = ["uv_build>=0.8.22,<0.9.0"]
build-backend = "uv_build"

[project]
name = "multick"
version = "0.0.0"
description = "An application to view a stocker ticker across multiple timeframes."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
