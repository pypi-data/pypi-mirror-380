[project]
name = "pilibr"
version = "0.0.1"
description = "A Python package for Pi calculations."
readme = "README.md"
authors = [{ name="nya~", email="noemail@example.com" }]
license = { text = "Apache-2.0" }
requires-python = ">=3"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"