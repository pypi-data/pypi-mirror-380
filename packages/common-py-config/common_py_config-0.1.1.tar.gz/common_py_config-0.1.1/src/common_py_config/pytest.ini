[pytest]
addopts =
    --cov=src
    --cov-report=term
    --cov-report=html:out/html
    -rA
    --basetemp=out/tests

# Optional: markers used in multiple projects
markers =
    slow: marks tests as slow
    integration: marks integration tests