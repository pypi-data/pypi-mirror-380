[build-system]
requires = ["setuptools>=61.1"]
build-backend = "setuptools.build_meta"

[project]
name = "pyetera-uart-bridge"
version = "1.0"
description = "Async python wrapper for etera-uart-bridge"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pyserial>=3.5"
]
authors = [
    {name = "Lenart Kos", email = "koslenart@gmail.com"}
]