[project]
name = "lite-logging-client"
version = "1.0.4"
description = "Lite logging client."
authors = [
    {name = "Ngoc-Do Tran", email = "dotrann.1412@gmail.com"},
]
dependencies = [
    "httpx",
]
requires-python = ">=3.8"
license = {text = "MIT"}

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["lite_logging"]