[project]
name = "mcp-macos"
version = "0.2.1"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Paul-Louis Pröve", email = "plp@workgenius.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.12.3",
]

[project.scripts]
mcp-macos = "mcp_macos.main:cli"

[build-system]
requires = ["uv_build>=0.8.8,<0.9.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]
