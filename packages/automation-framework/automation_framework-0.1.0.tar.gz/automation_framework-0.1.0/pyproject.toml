[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "automation-framework"              # package name shown on PyPI
version = "0.1.0"                          # initial release
description = "A Python framework for automation projects"
authors = [{ name="Paras", email="parasdattera555@gmail.com" }]
readme = "README.md"
license = {text = "MIT"}
dependencies = []

[project.urls]
Homepage = "https://github.com/yourusername/automation-framework"
