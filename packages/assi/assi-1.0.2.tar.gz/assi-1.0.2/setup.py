from setuptools import setup, find_packages 
 
setup( 
    name="assi", 
    version="1.0.2", 
    description="Advanced Scientific Computing Library", 
    packages=find_packages(), 
    python_requires=">=3.8", 
) 
