[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pztest"
version = "0.0.1"
authors = [
  { name="Your Name", email="you@example.com" },
]
description = "A harmless dummy package created for testing downloads."
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.8"
