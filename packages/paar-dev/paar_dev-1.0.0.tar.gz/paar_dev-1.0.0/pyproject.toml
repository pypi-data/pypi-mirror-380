[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "paar-dev"
version = "1.0.0"
description = "Biblioteca com funções específicas da Paar Desenvolvimentos"
authors = [{ name="Paar Desenvolvimentos", email="paardesenvolvimentos@gmail.com" }]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.8"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
