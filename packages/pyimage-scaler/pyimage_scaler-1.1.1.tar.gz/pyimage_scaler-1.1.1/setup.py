from setuptools import setup, find_packages

setup(
    name="pyimage_scaler",
    version="1.1.1",
    description="A unique Python package to resize images (JPEG, PNG, BMP, GIF)",
    author="codingmaster24",
    packages=find_packages(),
    python_requires=">=3.7",
)
