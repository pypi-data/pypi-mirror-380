[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "baseline-checker"
version = "0.1.0"
description = "Baseline Checker CLI tool for scanning folders/files with progress bar"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [{ name = "Veeresh Hanni", email = "veereshhanni347@gmail.com.com" }]

[project.scripts]
baseline-checker = "baseline_checker.baseline_checker:main"
