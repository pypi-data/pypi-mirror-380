[project]
name = "mkdocs-decodiff-plugin"
version = "0.0.5"
authors = [
  { name="kkAyataka", email="kk.ayataka@gmail.com" },
]
description = "MkDocs plugin to decorate git diff lines"
readme = "docs/README.md"
requires-python = ">=3.9"
classifiers = [
  "Development Status :: 4 - Beta",
  "Programming Language :: Python :: 3",
  "Operating System :: OS Independent",
]
license = "MIT"
license-files = ["LICENSE"]

[project.urls]
Homepage = "https://github.com/kkAyataka/mkdocs-decodiff-plugin"
Issues = "https://github.com/kkAyataka/mkdocs-decodiff-plugin"

[project.entry-points."mkdocs.plugins"]
decodiff = "mkdocs_decodiff_plugin.mkdocs_plugin.plugin:DecodiffPlugin"

[project.optional-dependencies]
dev = [
  "build", "twine", "wheel",
  "pytest", "pytest-cov",
  "ruff",
  "watchdog",
  "mkdocs"
]

# https://setuptools.pypa.io/en/latest/userguide/datafiles.html#package-data
[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
mkdocs_decodiff_plugin = ["**/*.css"]

[tool.ruff]
extend-select = ["I"]

[tool.ruff.isort]
combine-as-imports = true
