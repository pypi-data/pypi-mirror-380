[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "suture-run"
version = "0.1.3"
description = "Prompt Surgery"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
suture = "suture_run:main"

[tool.setuptools.packages.find]
where = ["src"]