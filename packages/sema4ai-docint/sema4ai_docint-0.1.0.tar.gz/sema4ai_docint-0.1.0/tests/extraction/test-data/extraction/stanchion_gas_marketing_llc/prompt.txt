For each deal, extract every daily transaction row that has its own date. Repeat the deal_id and location for each row. Do not collapse them into one, and do not skip rows if multiple dates exist under the same deal.