# SAKERNAS February 2024 wave-specific configuration
# Only contains fields that differ from base configuration

dataset: sakernas
version: '1.0'
wave: '2024_02'
extends: base.yaml

# Wave-specific metadata
metadata:
  reference_period: "February 2024"
  sample_design: "Two-stage stratified sampling"
  coverage: "National coverage, all provinces"
  age_coverage: "15 years and above"

# Override fields only if they differ from 2025 base
# Currently no overrides needed - 2024 uses same field structure as 2025
overrides: {}
