[project]
name = "freemework_abstractions"
version = "0.0.3"
description = "TBD"
authors = [
    { name = "Max Anurin", email = "maxim.anurin@freemework.org" },
]
dependencies = []
requires-python = ">=3.13"
readme = "README.md"

[project.license]
text = "MIT"

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[tool.pdm]
distribution = true

[dependency-groups]
test = [
    "pytest>=8.4.1",
]
