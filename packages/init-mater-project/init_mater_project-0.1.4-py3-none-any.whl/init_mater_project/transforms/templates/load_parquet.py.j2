# Parquet data
    df = pd.read_parquet("{{ dataset_path }}")
    print(f"Loaded {len(df)} records from Parquet file")

    # Optional: specify columns if needed
    # df = pd.read_parquet("{{ dataset_path }}", columns=["col1", "col2", "col3"])