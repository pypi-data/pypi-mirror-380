# CSV data
    df = pd.read_csv("{{ dataset_path }}", encoding="utf-8")
    print(f"Loaded {len(df)} records from CSV file")

    # Optional: specify columns if needed
    # df = pd.read_csv("{{ dataset_path }}", usecols=["col1", "col2", "col3"], encoding="utf-8")