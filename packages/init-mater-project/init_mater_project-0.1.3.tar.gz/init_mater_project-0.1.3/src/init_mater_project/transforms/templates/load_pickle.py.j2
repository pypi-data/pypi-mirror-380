# Pickle data
    with open("{{ dataset_path }}", "rb") as file:
        data = pickle.load(file)

    # Convert to DataFrame if needed
    if isinstance(data, pd.DataFrame):
        df = data
    else:
        df = pd.DataFrame(data)

    print(f"Loaded {len(df)} records from Pickle file")