# XML data
    # Parse XML - adjust structure as needed
    tree = ET.parse("{{ dataset_path }}")
    root = tree.getroot()

    # Convert to DataFrame - customize based on XML structure
    data = []
    for item in root:
        row = {child.tag: child.text for child in item}
        data.append(row)

    df = pd.DataFrame(data)
    print(f"Loaded {len(df)} records from XML file")