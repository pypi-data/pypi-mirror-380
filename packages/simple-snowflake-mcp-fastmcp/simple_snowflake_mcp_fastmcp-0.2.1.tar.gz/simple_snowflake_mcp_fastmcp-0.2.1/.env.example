# Example configuration for the MCP Snowflake server
SNOWFLAKE_USER=your_user
SNOWFLAKE_PASSWORD=your_password
SNOWFLAKE_ACCOUNT=your_account
# SNOWFLAKE_WAREHOUSE=your_warehouse  # Optional: Snowflake warehouse name
# SNOWFLAKE_DATABASE=your_database    # Optional: default database name
# SNOWFLAKE_SCHEMA=your_schema        # Optional: default schema name
# MCP_READ_ONLY=true                  # Optional: true/false to force read-only mode
