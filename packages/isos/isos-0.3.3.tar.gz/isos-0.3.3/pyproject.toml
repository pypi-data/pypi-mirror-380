[project]
name = "isos"
version = "0.3.3"
description = "Option and Result types for Python."
readme = "README.md"
authors = [{ name = "gsmyridis", email = "georgesmyr@icloud.com" }]
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = ["pre-commit>=4.2.0", "pytest>=8.3.5"]

[tool.setuptools.package-data]
"your_package" = ["py.typed"]
