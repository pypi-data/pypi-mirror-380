[tool.poetry]
name = "cie-restore"
version = "0.6.0"
description = ""
authors = ["Carmen Bianca BAKKER <carmen@coopiteasy.be>"]
maintainers = ["Coop IT Easy SC"]
license = "AGPL-3.0-or-later"
readme = "README.md"
repository = "https://gitlab.com/coopiteasy/cie-restore"

packages = [{include = "cie_restore"}]

[tool.poetry.scripts]
cie_download_archive = 'cie_restore.download:main'
cie_restore_db = 'cie_restore.db:main'
cie_restore_fs = 'cie_restore.fs:main'
cie_download_restore = 'cie_restore.all:main'

[tool.poetry.dependencies]
python = ">=3.8"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
