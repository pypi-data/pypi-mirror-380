[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mruclml"
version = "0.1.0"
description = "Print cool ASCII art in terminal with colors"
readme = "README.md"
requires-python = ">=3.7"
authors = [{name = "mruclml"}]
license = {text = "MIT"}