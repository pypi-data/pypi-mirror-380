[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "{name}"
version = "0.0.1"
dependencies = [
    "kuroboros=={kuroboros_version}"
]

[tool.setuptools.packages.find]
include = ["controllers"]