[project]
name = "handy-compute-mcp"
version = "0.1.5"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
       "build>=1.2.2.post1",
       "fastmcp>=2.12.3",
       "twine>=6.1.0"
]
[project.scripts]
compute_mcp_service = "compute_mcp_service:main"
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[tool.hatch.build.targets.wheel]
packages = ["src/compute_mcp_service"]