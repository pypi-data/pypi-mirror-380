[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "catfacts-cli"
version = "0.1.0"
description = "A fun CLI that shows random cat facts"
authors = [
    { name="Randomsy", email="RandomsySTZ@outlook.com" }
]
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.8"
dependencies = ["requests"]

[project.scripts]
catfacts = "catfacts.cli:main"
