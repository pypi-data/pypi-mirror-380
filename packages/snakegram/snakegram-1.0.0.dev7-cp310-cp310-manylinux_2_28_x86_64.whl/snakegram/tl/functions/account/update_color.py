
#                     !!! WARNING !!!
#             This is an auto-generated file!
#                 Generated by [Builder].
# Any manual modifications will be lost upon regeneration.
#             DO NOT EDIT THIS FILE DIRECTLY.

from typing import Optional
from ....gadgets.tlobject import TLRequest
from ....gadgets.byteutils import Writer


class UpdateColor(TLRequest[bool]):
    """
    Update the [accent color and background custom emoji](https://core.telegram.org/api/colors) of the current account.

    Raises:
        `400`ColorInvalidError: The specified color palette ID was invalid.
        `403`PremiumAccountRequiredError: A premium account is required to execute this action.

    """

    _id = 0X7CEFA15D
    _result_id = 0X43B904E1

    def __init__(
        self,
        for_profile: bool = False,
        color: Optional[int] = None,
        background_emoji_id: Optional[int] = None
    ):

        """
        Update the [accent color and background custom emoji](https://core.telegram.org/api/colors) of the current account.

        Args:
            for_profile (bool, optional): Whether to change the accent color emoji pattern of the profile page; otherwise, the accent color and emoji pattern of messages will be changed.
            color (Optional[int], optional): [ID of the accent color palette](https://core.telegram.org/api/colors) to use (not RGB24, see [here](https://core.telegram.org/api/colors) for more info).
            background_emoji_id (Optional[int], optional): Custom emoji ID used in the accent color pattern.
        """

        self.for_profile = for_profile
        self.color = color
        self.background_emoji_id = background_emoji_id

    def to_bytes(self, boxed: bool=True):
        with Writer(b']\xa1\xef|' if boxed else b'') as writer:
            with writer.flag() as flags:
                flags(self.for_profile, 1)

                if flags(self.color, 2):
                    writer.int(self.color)

                if flags(self.background_emoji_id, 0):
                    writer.long(self.background_emoji_id)

            return writer.getvalue()