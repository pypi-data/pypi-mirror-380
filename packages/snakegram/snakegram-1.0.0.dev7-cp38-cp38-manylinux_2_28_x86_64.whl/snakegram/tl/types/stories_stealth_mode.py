
#                     !!! WARNING !!!
#             This is an auto-generated file!
#                 Generated by [Builder].
# Any manual modifications will be lost upon regeneration.
#             DO NOT EDIT THIS FILE DIRECTLY.

from typing import Optional, TYPE_CHECKING
from ...gadgets.tlobject import TLObject
from ...gadgets.byteutils import Writer


if TYPE_CHECKING:
    from ...gadgets.byteutils import Reader


class StoriesStealthMode(TLObject, family='TypeStoriesStealthMode'):
    """
    Information about the current [stealth mode](https://core.telegram.org/api/stories#stealth-mode) session.
    """

    _id = 0X712E27FD
    _group_id = 0XFAE6BCB9

    def __init__(
        self,
        active_until_date: Optional[int] = None,
        cooldown_until_date: Optional[int] = None
    ):

        """
        Information about the current [stealth mode](https://core.telegram.org/api/stories#stealth-mode) session.

        Args:
            active_until_date (Optional[int], optional): The date up to which stealth mode will be active.
            cooldown_until_date (Optional[int], optional): The date starting from which the user will be allowed to re-enable stealth mode again.
        """

        self.active_until_date = active_until_date
        self.cooldown_until_date = cooldown_until_date

    def to_bytes(self, boxed: bool=True):
        with Writer(b"\xfd'.q" if boxed else b'') as writer:
            with writer.flag() as flags:

                if flags(self.active_until_date, 0):
                    writer.int(self.active_until_date)

                if flags(self.cooldown_until_date, 1):
                    writer.int(self.cooldown_until_date)

            return writer.getvalue()

    @classmethod
    def from_reader(cls, reader: 'Reader'):
        flags = reader.flag()
        active_until_date_ = (
            reader.int()
            if flags(0) else None
        )

        cooldown_until_date_ = (
            reader.int()
            if flags(1) else None
        )

        return cls(
            active_until_date=active_until_date_,
            cooldown_until_date=cooldown_until_date_
        )

TypeStoriesStealthMode = StoriesStealthMode