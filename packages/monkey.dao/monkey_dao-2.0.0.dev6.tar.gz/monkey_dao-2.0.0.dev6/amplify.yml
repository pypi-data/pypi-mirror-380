version: 1
frontend:
  phases:
    preBuild:
      commands:
        - python --version
        - python -m pip install --upgrade pip
        - pip install . --group all
    build:
      commands:
        - mkdocs build
  artifacts:
    baseDirectory: build/site
    files:
      - '**/*'
  cache:
    paths: [ ]
