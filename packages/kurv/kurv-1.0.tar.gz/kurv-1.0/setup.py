from setuptools import setup, find_packages

setup(
    name="kurv",
    version="1.0",
    packages=find_packages(),
    description="Kurv programming language in Python",
    author="Kurv",
    python_requires=">=3.8",
)
