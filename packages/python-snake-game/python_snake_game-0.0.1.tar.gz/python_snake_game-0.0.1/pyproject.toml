[project]
name = "python-snake-game"
version = "0.0.1"
description = "Reserved name for future project."
authors = [{ name="ramimK0bir", email="kobirbiddut81@gmail.com" }]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.7"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
